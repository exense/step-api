package step.grid.io.stream.download;

import step.grid.io.stream.JsonMessage;

// Messages sent between (websocket) download server and client
public class DownloadProtocolMessage extends JsonMessage {
}
